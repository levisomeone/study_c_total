# Linguagem Estruturada
Estudos baseados no livro C completo e total do autor Herbert Schildt 

Diferença de Variáveis, Global e Local
//

    void func1(void){
        int x;
        x = 10;
        printf("%d", x); // x = 10
    }
    printf("%d", x); // x = nada
    
    void func2(void){
        int = x;
        x = -199;
        printf("%d", x); // x = -199
    }
    return  0;
    
  as diferenças..
